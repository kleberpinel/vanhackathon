require 'spec_helper'

describe CrawlerService do
  # describe "#call" do
  #   let(:ip) { double }
  #   let(:transaction) { create(:transaction) }

  #   it "returns the result of #update_attributes! call" do
  #     result = double

  #     allow(transaction).to receive(:update_attributes!).and_return(result)
  #     expect(described_class.new(transaction, ip).call).to eq(result)
  #   end
  # end
end
